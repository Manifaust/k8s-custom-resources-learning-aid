
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: checkweathers.weather-app.example.com
spec:
  group: weather-app.example.com
  names:
    kind: CheckWeather
    listKind: CheckWeatherList
    plural: checkweathers
    singular: checkweather
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: CheckWeather is the Schema for the checkweathers API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: CheckWeatherSpec defines the desired state of CheckWeather
          properties:
            city:
              description: The city to check the weather for
              type: string
          type: object
        status:
          description: CheckWeatherStatus defines the observed state of CheckWeather
          properties:
            state:
              description: 'Important: Run "make" to regenerate code after modifying
                this file'
              type: string
            temperature:
              format: int32
              type: integer
          required:
          - temperature
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
